### 核心定义（最易懂版）

**mipmap（多级纹理映射）** 是游戏 / 图形渲染中一种**优化纹理显示**的技术，简单说就是给一张原始纹理（比如游戏里的墙面、地面贴图），提前生成一系列 “从小到大、清晰度递减” 的缩略图（叫 mip 层），渲染时根据物体离镜头的远近来自动选择对应清晰度的贴图。

比如：

- 物体离镜头近 → 用高清原始图（不模糊）
- 物体离镜头远 → 用小尺寸缩略图（省性能，还能避免画面锯齿、闪烁）